---
title: Sidepanel package
description: Add the DocSearch Sidepanel to a React application.
---

import TabItem from '@theme/TabItem';
import Tabs from '@theme/Tabs';

`typesense-docsearch-sidepanel` provides composable React components for an Agent Studio chat panel.

## Install

<Tabs groupId="package-manager" aria-label="Package manager">
  <TabItem value="npm" label="npm">

```bash
npm install typesense-docsearch-core@^5 typesense-docsearch-sidepanel@^5 typesense-docsearch-css@^5
```

  </TabItem>
  <TabItem value="yarn" label="Yarn">

```bash
yarn add typesense-docsearch-core@^5 typesense-docsearch-sidepanel@^5 typesense-docsearch-css@^5
```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

```bash
pnpm add typesense-docsearch-core@^5 typesense-docsearch-sidepanel@^5 typesense-docsearch-css@^5
```

  </TabItem>
  <TabItem value="bun" label="Bun">

```bash
bun add typesense-docsearch-core@^5 typesense-docsearch-sidepanel@^5 typesense-docsearch-css@^5
```

  </TabItem>
</Tabs>

## Render the Sidepanel

Import both the main DocSearch stylesheet and the Sidepanel stylesheet.

```tsx title="HelpAssistant.tsx"
import { DocSearch } from 'typesense-docsearch-core';
import { Sidepanel, SidepanelButton } from 'typesense-docsearch-sidepanel';

import 'typesense-docsearch-css';
import 'typesense-docsearch-css/dist/sidepanel.css';

export function HelpAssistant() {
  return (
    <DocSearch>
      <SidepanelButton />
      <Sidepanel
        appId="YOUR_APPLICATION_ID"
        apiKey="YOUR_SEARCH_API_KEY"
        agentId="YOUR_AGENT_ID"
      />
    </DocSearch>
  );
}
```

Use a search-only API key. The `agentId` is an Agent Studio agent ID. V5 doesn't provide the legacy Ask AI backend or an `agentStudio` switch.

The default button and panel float over the page. Press `Control+I` or `Command+I` to toggle the panel, and press `Escape` to close it.

## Configure the provider

Set the theme and lifecycle callbacks on `DocSearch`.

```tsx title="HelpAssistant.tsx"
<DocSearch
  theme="dark"
  keyboardShortcuts={{ 'Ctrl/Cmd+I': true }}
  onSidepanelOpen={() => track('assistant_opened')}
  onSidepanelClose={() => track('assistant_closed')}
>
  <SidepanelButton variant="inline" />
  <Sidepanel
    appId="YOUR_APPLICATION_ID"
    apiKey="YOUR_SEARCH_API_KEY"
    agentId="YOUR_AGENT_ID"
  />
</DocSearch>
```

Continue with [advanced use cases](./advanced-use-cases) or review the [Sidepanel API](./api). For a combined search and assistant experience, see [Hybrid Mode](/docs/hybrid-mode).
